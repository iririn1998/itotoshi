{
  "name": "@itotoshi/game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "oxlint -c ../.oxlintrc.json",
    "format": "oxfmt -c ../.oxfmtrc.json --write .",
    "format:check": "oxfmt -c ../.oxfmtrc.json --check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@itotoshi/ranking-contract": "workspace:*",
    "excalibur": "^0.32.0"
  },
  "devDependencies": {
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vitest": "^5.0.1"
  }
}
